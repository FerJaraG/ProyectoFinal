<div class="gtco-section border-bottom">
		<div class="gtco-container">
			<div class="row">
				<div class="col-md-12">
					<div class="col-md-6 animate-box">
					<h3>Sitio:</h3>
					<%= form_with(model: [@camping, @campsite], local: true) do |f| %>

						<div class="row form-group">
							<div class="col-md-12">
								<%= f.label :identifier,'Codigo:' %>
                                <%= f.text_field :identifier, autofocus: true, class: "form-control"%>
                            </div>
						</div>

						<div class="row form-group">
							<div class="col-md-12">
                                <%= f.label :description, 'Descripción:' %>
                                <%= f.text_area :description, class: "form-control", cols: "30", rows: "5" %>
                            </div>
						</div>

                        <div class="row form-group">
							<div class="col-md-12">
                                <%= f.label :status, 'Ocupado:' %>
                                <%= f.check_box :status%>
                            </div>
						</div>

						<div class="form-group">
                            <%= f.submit 'Guardar', class: "btn btn-primary"  %>
						</div>

					</form>	
                    <% end %>	
				</div>
				</div>
			</div>
		</div>
	</div>