
/*
jQuery(function() {
  var cities;
  cities = $('#city_id').html();
  console.log(regions);
  return $('#region_id').change(function() {
    var region, options;
    region = $('#region_id :selected').text();
    options = $(cities).filter("optgroup[label=" + region + "]").html();
    console.log(options);
    if (options) {
      return $('#city_id').html(options);
    } else {
      return $('#city_id').empty();
    }
  });
});

*/


